{
    "version": 2,
    "buildCommand": "pip install -r requirements.txt",
    "installCommand": "pip install -r requirements.txt",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "app.py"
      }
    ],
    "python": {
      "runtime": "python3.9"
    }
  }
  